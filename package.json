{
  "name": "football-radar",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "schedule:once": "node --env-file-if-exists=.env.local --import tsx scripts/schedule-once.ts",
    "push:now": "node --env-file-if-exists=.env.local --import tsx scripts/push-now.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^20",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
