# dependencies
node_modules

# misc
.DS_Store

# next.js artifacts (项目已移除 next，但有遗留 .next 目录的话也忽略)
.next/

# debug
npm-debug.log*

# env files
.env*
!.env.example

# SQLite WAL/SHM 临时文件不进 git；主 db 进 git 当持久化（GH Actions 部署需要）
data/*.db-shm
data/*.db-wal

# typescript
*.tsbuildinfo
